import React from 'react';

const Product = (props) => {
  return (
    <div>
      Product
      <img src={require('../img/logo.svg')} alt="" width={100} height={100} />
    </div>
  );
};

export default Product;
